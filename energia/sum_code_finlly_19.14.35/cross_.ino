int mySemaphore1=0;
int mySemaphore2=0;
int count_now;
void setupcross_setup(){
   
}
void loopcross_(){
if(mySemaphore1==1){
  brake(mor1,mor2);
  while(count_now==count_div_digital_recognition)
  if(contrl_flag=='L')
     {rote[count]='L';
      turn_car(mor1,mor2,L_bit);
      count++;
     }
     if(contrl_flag=='R')
     {rote[count]='R';
      turn_car(mor1,mor2,R_bit);
      count++;
     }
      if(contrl_flag=='G')
      { rote[count]='G';
         count++;
       }
  mySemaphore1=0;
  
  }              
}
